import os

# --- Telegram ---
BOT_TOKEN = os.environ["BOT_TOKEN"]
# Заполняется после первого /start у бота (бот сам подскажет твой chat_id).
CHAT_ID = os.environ.get("CHAT_ID") or None

# Во сколько присылать расписание на завтра автоматически (по TIMEZONE ниже).
SEND_HOUR = int(os.environ.get("SEND_HOUR", "20"))
SEND_MINUTE = int(os.environ.get("SEND_MINUTE", "0"))
TIMEZONE = os.environ.get("TIMEZONE", "Europe/Moscow")

# --- Источник расписания ---
GROUP_ID = 474739
BASE_URL = "https://timetable.spbu.ru"
GROUP_PATH = f"/FL/StudentGroupEvents/Primary/{GROUP_ID}"

# --- Правила отображения предметов ---
# Список проверяется по порядку сверху вниз. Для каждого предмета с сайта
# берётся ПЕРВОЕ подходящее правило:
#   match     — с этого текста должно начинаться название предмета (как оно
#               приходит с сайта, вместе с "лекция"/"практическое занятие")
#   subgroup  — необязательно; если указано, правило применяется только к
#               занятиям этой подгруппы (сайт отдаёт номер подгруппы отдельно)
#   exclude   — True, значит предмет полностью скрывается из расписания
#   short     — сокращённое название для вывода; если не задано и предмет не
#               исключён, в вывод попадает название как есть с сайта
#
# Правила с exclude должны идти РАНЬШЕ более общих правил для того же
# предмета (см. китайский язык ниже) — иначе исключение не сработает.
RULES = [
    {"match": "Электив. Испанский язык", "exclude": True},
    {"match": "Электив. Итальянский язык", "exclude": True},
    {"match": "Электив. Китайский язык, практическое занятие", "subgroup": "2", "exclude": True},

    {"match": "Актуальные проблемы международного права", "short": "международное право"},
    {"match": "Основы синхронного перевода, практическое занятие", "short": "синхронный перевод"},
    {"match": "Устный судебный перевод, практическое занятие", "short": "устный перевод"},
    {"match": "Общая теория перевода, лекция", "short": "теория перевода"},
    {"match": "Электив. Китайский язык, практическое занятие", "short": "китайский язык"},
    {"match": "Французский язык, практическое занятие", "short": "французский"},
    {"match": "Устный перевод в профессиональной сфере, практическое занятие", "short": "устный перевод"},
    {"match": "Английский язык в профессиональной сфере, практическое занятие", "short": "английский"},
    {"match": "Методика преподавания иностранных языков, лекция", "short": "методика преподавания"},
]
